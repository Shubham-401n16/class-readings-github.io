# class-readings-02-github.io

What is a data structure?
A data structure is a particular way of organizing data in a computer so that it can be used effectively.

How is a Linked List different from an array?
An array tends to be more ridgid; adding to an array or deleting from an array can be costly.  
In older languages like Java and C++, arrays do not easily change size.
Linked Lists can be a nice alternative in those languages, since it is quite easy to add or remove Nodes.

What is one benefit a Linked List has over an array?

Data can be removed from nodes easily compared to from arrays.

What data does a Node hold in a doubly Linked List?

In doubly linked list node holds the link to the next and previous nodes

What would you use to implement a Linked List data type? (object, function, class, variable…?)

A linkedlist data type can be any of these.
